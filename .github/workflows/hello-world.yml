name: Hello World Workflow

on:
  # 触发工作流程的事件
  # push:
  #   branches: [ main ]
  # pull_request:
  #   branches: [ main ]
  # 允许手动触发工作流程
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    
    steps:
    - name: Print Hello World
      run: echo "Hello World!" 